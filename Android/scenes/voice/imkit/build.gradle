apply from: '../voice.gradle'

android {
    sourceSets {
        main {
            jniLibs.srcDirs = ['src/main/jniLibs']
        }
    }
}

dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])
    implementation project(":scenes:voice:buddy")
    api rootProject.ext.deps.core_ktx
    api rootProject.ext.deps.agora_chat
}